Fleet TYPO3
===========

The command line utility to provide system information for the [Fleet Master](https://github.com/cundd/fleet_master.git).

Usage
-----

The extension only provides a Extbase CLI command to print the information as JSON:

```bash
php typo3/cli_dispatch.phpsh extbase fleet:info:info
```
